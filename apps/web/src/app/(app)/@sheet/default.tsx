/**
 * Default component for the parallel route slot.
 *
 * This component renders nothing and acts as a fallback when no other route is matched.
 * Used in Next.js parallel routing to define a default content placeholder.
 *
 * @returns {null} This component renders nothing.
 */
export default function DefaultSlot() {
  return null
}
